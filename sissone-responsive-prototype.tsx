import { View, Text } from "react-native"

const v0 = "v0 value"
const no = "no value"
const op = "op value"
const code = "code value"
const block = "block value"
const prefix = "prefix value"

const SissoneResponsivePrototype = () => {
  return (
    <View>
      <Text>{v0}</Text>
      <Text>{no}</Text>
      <Text>{op}</Text>
      <Text>{code}</Text>
      <Text>{block}</Text>
      <Text>{prefix}</Text>
      {/* rest of code here */}
    </View>
  )
}

export default SissoneResponsivePrototype
